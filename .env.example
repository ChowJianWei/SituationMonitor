# Database
PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Email
RESEND_API_KEY="re_..."

# External APIs
FINNHUB_API_KEY="your-finnhub-key"
LLM_API_KEY="your-llm-key" # Optional

# Quant service (options/vol analysis — runs as separate container)
QUANT_SERVICE_URL="http://localhost:8000"
FRED_API_KEY="" # Optional — FRED DGS3MO series works without a key

# Actuarial quant engine (FastAPI) — runs as its own 24/7 service, NOT on Vercel.
# Local dev: http://localhost:8200. Production: the deployed engine URL (Railway/Fly).
ACTUARIAL_API_URL="http://localhost:8200"
